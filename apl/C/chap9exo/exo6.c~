#include <stdio.h>

int sumsquare(int n) {

  if (n==0)
    return 0;
  else if(n==1)
    return 1;
  else 
    return sumsquare(n-1)+n*n;
}

int main() {

  int res=sumsquare(2);
  printf("%d\n",res);

  return 0;

}
