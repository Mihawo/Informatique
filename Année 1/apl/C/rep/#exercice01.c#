#include <stdio.h>

int f1() {
  double d1, d2;
  scanf("%lf", &d1);
  scanf("%lf", &d2);   
  
  if (d1 < 0 && d2 < 0 || d1 >= 0 && d2 >= 0)
    printf("+\n");
  else
    printf("-\n");   
  
  return 1;
}
